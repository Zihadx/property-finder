# ListEasy BD — environment variables
# Currently unused (frontend-first, mock data). Populate when backend/API is connected.

# NEXT_PUBLIC_API_BASE_URL=
# DATABASE_URL=
# NEXTAUTH_SECRET=
# NEXTAUTH_URL=
